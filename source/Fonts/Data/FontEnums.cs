namespace NanoUI.Fonts.Data
{
    internal enum CodepointType
    {
        Space,
        Newline,
        Char,
        CJKChar
    }

    internal enum FontGlyphBitmap
    {
        Optional = 1,
        Required = 2
    }
}